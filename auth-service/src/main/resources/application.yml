# Server Configuration
server:
  port: 8082

spring:
  application:
    name: auth-service
  cloud:
    config:
      enabled: false

# JWT Configuration
jwt:
  secret: myVerySecureSecretKeyThatIsAtLeast32CharactersLong123456789
  expiration: 86400000 # 24 hours in milliseconds

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

